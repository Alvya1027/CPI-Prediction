# 孪生回归样本对构造报告

生成时间：2026-07-15 14:04

## 1. 样本对总数

| 数据集 | 样本对数量 |
|--------|-----------:|
| 训练集 | 307 |
| 验证集 | 71 |
| 测试集 | 44 |
| **合计** | **422** |

## 2. CPI 差值区间划分

训练集 delta_cpi 分位阈值：q1 = -0.8000，q2 = 1.6500

| 区间 | 范围 | 训练集 | 验证集 | 测试集 |
|------|------|-------:|-------:|-------:|
| small | delta_cpi ≤ -0.8000 | 98 | 29 | 21 |
| medium | -0.8000 < delta_cpi ≤ 1.6500 | 112 | 22 | 15 |
| large | delta_cpi > 1.6500 | 97 | 20 | 8 |

**较大 CPI 差值区间（large）样本总数：125 对**

## 3. 相似标签分布

| 数据集 | 相似（1） | 不相似（0） | 含跳变已剔除 |
|--------|----------:|------------:|-------------:|
| 训练集 | 146 | 161 | 0 |
| 验证集 | 38 | 33 | 0 |
| 测试集 | 20 | 24 | 0 |

## 4. 时间泄漏检查

| 检查项 | 训练集 | 验证集 | 测试集 |
|--------|--------|--------|--------|
| 无时间泄漏且窗口间隔≥12月 | 通过 | 通过 | 通过 |

说明：
- target_j_date 必须早于 target_i_date，确保参考 CPI 在预测时已知。
- x_i_start_date 与 x_j_end_date 至少间隔 12 个月，确保两个 12 个月窗口不重叠。

## 5. 样本对字段说明

| 字段 | 说明 |
|------|------|
| pair_id | 样本对编号 |
| sample_i_id | 目标窗口在原始样本中的索引 |
| sample_j_id | 参考窗口在原始样本中的索引 |
| x_i_start_date | 目标窗口起始月份 |
| x_i_end_date | 目标窗口结束月份 |
| target_i_date | 目标 CPI 月份 |
| x_j_start_date | 参考窗口起始月份 |
| x_j_end_date | 参考窗口结束月份 |
| target_j_date | 参考 CPI 月份 |
| cpi_i | 目标下一个月 CPI |
| cpi_j | 参考下一个月 CPI |
| delta_cpi | cpi_i - cpi_j（回归标签） |
| delta_bin | delta_cpi 所属区间：small / medium / large |
| similar_label | 可选相似标签：1=相似，0=不相似，-1=含跳变已剔除 |

## 6. 相似性筛选规则

### 6.1 跳变检测

1. 对 12 个月 CPI 片段内部做 z-score 标准化。
2. 计算标准化后序列的一阶差分。
3. 对一阶差分再做 z-score，若任一 \|z\| > 2.0，则判定该片段含跳变。

### 6.2 趋势方向判定

| 方向 | 线性斜率条件 |
|------|-------------|
| 上升 | trend > 0.05 |
| 下降 | trend < -0.05 |
| 平稳 | -0.05 ≤ trend ≤ 0.05 |

### 6.3 标签规则

- similar_label = 1：两个片段趋势方向相同，且都不含跳变。
- similar_label = 0：两个片段趋势方向不同，或任一片段含跳变。
- similar_label = -1：任一片段含跳变，该样本对将被剔除。

注意：similar_label 仅用于样本筛选和参考时期筛选，不作为孪生回归网络的最终训练目标。最终训练目标为 delta_cpi。

## 7. 生成脚本

```bash
python -m src.create_siamese_pairs
```
